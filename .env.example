# Environment variables template for e-commerce microservices
# Copy this file to .env and populate with your own values

# MongoDB credentials
MONGO_INITDB_ROOT_USERNAME=ecommerce_admin
MONGO_INITDB_ROOT_PASSWORD=secure_password_here

# MongoDB connection URI (optional - will auto-compose from credentials if not provided)
# Format: mongodb://<username>:<password>@mongo:27017/<database>
# Leave empty to use auto-composed URI in Docker Compose
MONGO_URI=

# MongoDB database name
MONGO_DATABASE=ecommerce

# Service ports (DO NOT CHANGE - required by project)
BACKEND_PORT=3847
GATEWAY_PORT=5921

# Backend URL for gateway (optional - will default to http://backend:3847 if not set)
# Use this to override backend location when running outside Docker or in special network setups
BACKEND_URL=

# Node environment
NODE_ENV=development
